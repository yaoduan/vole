server:
  port: 4001

spring:
  application:
    name: vole-config-server
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: http://zyd.synology.me:8010/wangyaoduan/springcloudconfig.git
          search-paths: respo
          username: wangyaoduan
          password: HeWan0519

eureka:
  instance:
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 5
    lease-expiration-duration-in-seconds: 20
  client:
    serviceUrl:
      defaultZone: http://vole-eureka-node1:1026/eureka,http://vole-eureka-node2:1027/eureka
    registry-fetch-interval-seconds: 10
